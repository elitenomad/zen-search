# frozen_string_literal: true

RSpec.describe Zen::Search do
  it "has a version number" do
    expect(Zen::Search::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
